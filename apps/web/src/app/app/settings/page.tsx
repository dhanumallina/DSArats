import { ComingSoon } from "@/components/ui/coming-soon";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      phase="Profile editing arrives in Phase 3"
      description="Update your username, display name, bio, learning goal, and preferences."
    />
  );
}